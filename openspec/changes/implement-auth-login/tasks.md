# Tasks: Implement Auth Flows

- [ ] Scaffold `pages/login.vue` and `pages/signup.vue`.
- [ ] Implement `LuxuryInput` and `LuxuryButton` components.
- [ ] Update `stores/auth.ts` to include `register` and `login` actions.
  - [ ] Ensure NO client-side validation in these actions.
- [ ] Implement Google Auth action (upsert).
- [ ] Wire up `/login` UI with "Sign In" and "Continue with Google".
- [ ] Wire up `/signup` UI with "Sign Up" and "Continue with Google".
- [ ] Add navigation links between Login and Signup pages.
- [ ] Apply Luxury theme styling to both pages.
