# Tasks: Implement Auth Flows

- [x] Scaffold `pages/login.vue` and `pages/signup.vue`.
- [x] Implement `LuxuryInput` and `LuxuryButton` components.
- [x] Update `stores/auth.ts` to include `register` and `login` actions.
  - [x] Ensure NO client-side validation in these actions.
- [x] Implement Google Auth action (upsert).
- [x] Wire up `/login` UI with "Sign In" and "Continue with Google".
- [x] Wire up `/signup` UI with "Sign Up" and "Continue with Google".
- [x] Add navigation links between Login and Signup pages.
- [x] Apply Luxury theme styling to both pages.
